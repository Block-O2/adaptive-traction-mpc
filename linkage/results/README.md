# Linkage Results

- `results/local/` is entirely ignored by Git.
- Formal conclusions are recorded in the tracked reports under `../docs/`.
- Local MAT, GIF, PNG, FIG, CSV, log, and workspace files are original
  experiment artifacts and are not deleted by this cleanup.
- Retained V1 local results can be interpreted and reproduced with the archived
  implementation at annotated tag `linkage-pre-v1-code-cleanup`.
- `mujoco_sleeve_robot_v2/` is the tracked, compact V2 engineering-validation
  evidence package (summary, CSV tables, compressed traces, and representative
  figures). Exploratory reruns remain under ignored `results/local/` paths.
